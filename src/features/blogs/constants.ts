/**
 * Constants for the blogs feature
 * Following the SSOT (Single Source of Truth) principle
 */

export const COMMENT_LIMITS = {
  COMMENT_MAX_LENGTH: 5000,
  COMMENT_MIN_LENGTH: 1,
  EMAIL_MAX_LENGTH: 255,
  NAME_MAX_LENGTH: 100,
  NAME_MIN_LENGTH: 1,
} as const
